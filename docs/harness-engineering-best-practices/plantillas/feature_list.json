{
  "$schema_note": "PLANTILLA de feature list. Cada feature DEBE tener el trío: behavior + verification + state. Estados: not_started | active | blocked | passing. Solo UNA feature 'active' a la vez (WIP=1). Las transiciones a 'passing' las controla el harness/script de verificación, no el agente a mano. Ver docs/06-feature-lists.md",
  "project": "<nombre del proyecto>",
  "wip_limit": 1,
  "features": [
    {
      "id": "F01",
      "behavior": "POST /api/register con {email, password} devuelve 201 y crea el usuario",
      "verification": "curl -s -X POST http://localhost:3000/api/register -H 'Content-Type: application/json' -d '{\"email\":\"test@example.com\",\"password\":\"123456\"}' | jq -e '.status == 201'",
      "state": "passing",
      "evidence": "commit abc123, log de salida del test",
      "depends_on": []
    },
    {
      "id": "F02",
      "behavior": "POST /api/cart/items con {product_id, quantity} devuelve 201 y agrega el ítem al carrito",
      "verification": "curl -s -X POST http://localhost:3000/api/cart/items -H 'Content-Type: application/json' -d '{\"product_id\":1,\"quantity\":2}' | jq -e '.status == 201'",
      "state": "active",
      "evidence": "",
      "depends_on": ["F01"]
    },
    {
      "id": "F03",
      "behavior": "El flujo de checkout end-to-end completa una compra y deja la orden en estado 'paid'",
      "verification": "playwright test tests/e2e/checkout.spec.ts",
      "state": "blocked",
      "evidence": "",
      "blocked_reason": "Falta integrar el proveedor de pagos (clave de sandbox no disponible)",
      "depends_on": ["F02"]
    },
    {
      "id": "F04",
      "behavior": "<descripción de un solo comportamiento, completable en una sesión>",
      "verification": "<comando ejecutable que devuelve éxito/fracaso>",
      "state": "not_started",
      "evidence": "",
      "depends_on": []
    }
  ]
}
