# Plantilla de variables de entorno — copiar a `.env.local` y completar con valores reales.
# NUNCA commitear `.env.local` con valores: `.gitignore` ya lo cubre (#2 — secretos desde env).

# Credencial del LLM (OpenAI). En producción vive como env var del proyecto en Vercel, no en el repo.
OPENAI_API_KEY=
